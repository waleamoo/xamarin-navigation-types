<?xml version="1.0" encoding="UTF-8"?>
<ContentPage Title="Introduction" xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="NavigationTypes.IntroductionPage"
             Padding="0, 20, 0, 0">
    <!--
    To Remove the default Back Button = In the <ContentPage NavigationPage.HasBackButton="false">

    // to disable device back button

    protected override bool OnBackButtonPressed()
    {
        return true;
    }

    -->
    <StackLayout HorizontalOptions="Center" VerticalOptions="Center">
        <Label Text="This is how the app works." HorizontalOptions="Center" />
        <Button Text="Back" Clicked="Button_Clicked" />
    </StackLayout>
</ContentPage>
